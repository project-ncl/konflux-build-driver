quarkus:
  oidc:
    enabled: false
  oidc-client:
    enabled: false
  log:
    category:
      "org.jboss.pnc.konfluxbuilddriver":
        level: DEBUG
konflux-build-driver:
  pipeline-resolver: "foobar"
